//all 3 are the same

export const arrowFunctionA = (x: number, y: number) => {
    return x + y;
};

export const arrowFunctionB = (x: number, y: number) => x + y;

export function function3(x: number, y: number){
    return x + y;
}